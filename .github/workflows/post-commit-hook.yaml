name: "Post Commit Hook"

on:
  push:
    branches-ignore:
      - main

run-name: "[${{ github.sha }}] build"

jobs:
  run-build:
    uses: kgusarov/cilib/.github/workflows/post-commit-hook.yaml@main
    secrets: inherit
